scalar JSON

type GetCurationProductResponse {
  ok: Boolean!
  error: String
  product: [Product]
}

type Query {
  GetCurationProduct(sellerId: Int, answeredJson: JSON): GetCurationProductResponse!
}
